# OCTOPUS OMNI COCKPIT — Project Instructions

## Project Overview
**OCTOPUS v2.0** — Plataforma SaaS de automatización IA para negocios.
- **Owner**: Rafael Angel (CEO, Octopus Skills)
- **Email admin**: 1billontopview@gmail.com (ilimitado en plan-gate)
- **Email empresa**: contact@octopuskills.com
- **Idioma**: SIEMPRE español en respuestas. Tono directo, emoji 🐙 ocasional.

## Deployed URLs (sin deployment_tag — deployar por separado)
- **octopuskills.com**
- **octopus-omni-cockpit-n8hd61.abacusai.app**

## Architecture
- 250 API routes, 40+ pages, 33 módulos en sidebar
- Prisma ORM con PostgreSQL
- NextAuth (Google SSO + Credentials)
- RAG 2.0 knowledge base, LLM via Abacus AI + Turbo Mode (OpenRouter)
- Framer Motion animations, Tailwind CSS
- i18n bilingüe EN/ES

## Major Update: v2.0 (June 12, 2026)
Full code replacement from Claude Code (branch claude/zen-lovelace-ai8zlo).
### New modules added:
- 🫧 Burbujas (Mapa Mental Infinito) — bubbles-mind-map.tsx
- 🔌 MCP Server (9 herramientas) — /api/mcp, /dashboard/mcp-server
- ⚡ Cockpit (KPIs real-time) — /dashboard/cockpit
- 🔄 Automation List (reemplaza Social Bridge) — /dashboard/automation
- 📱 Omnicanal (Telegram/WhatsApp/SMS) — /dashboard/channels
- 🧬 Canvas Templates marketplace — /dashboard/canvas-templates
- Canvas deploy con 4 opciones: OCTOPUS/GitHub/Hostinger/Plantilla

### Schema changes: NONE (identical, safe for production data)
### TypeScript fixes: 2 null assertion fixes in automation/login and canvas/vision routes

## Major Update: v2.1 — Fable 5 Power Upgrade (June 13, 2026)
ZIP: manifiesto-claude-zen-lovelace-ai8zlo el nuevo poder es este 11.zip
### New features from Fable 5:
- Code Engine Runtime (WebContainer-based) — /dashboard/claude-code/runtime
- VPS Deploy capability — /api/arms/vps, /api/arms/claude-code/vps-deploy
- Test scaffolding — /api/arms/claude-code/generate-tests
- Code Engine runtime boot — /api/arms/claude-code/runtime-boot
- Monaco editor modal — components/code-editor-modal.tsx
- Token tracking system — lib/token-tracking.ts
- Premium design system — lib/premium-design-system.ts
- SaaS scaffold — lib/saas-scaffold.ts
- Incremental diff — lib/incremental-diff.ts
- Specialist lenses — lib/skills/specialist-lenses.ts
- Ensure schema — lib/ensure-schema.ts
### Schema changes (additive, safe):
- JarvisSession: +promptTokens, +completionTokens, +totalTokens, +estCostUsd (all @default(0))
- JarvisMessage: +promptTokens, +completionTokens (all @default(0))
### New dependencies: @monaco-editor/react, @webcontainer/api, node-ssh
### Fable 5 implemented the 🧠 model selector natively (confirmed in ZIP)
### TypeScript fixes: 2 null assertion fixes in automation/login and canvas/vision routes

## Feature Backlog (Rafael requests)
- **Selector de Modelo en Chat**: DESPLEGADO (June 12, 2026) — Botón 🧠 (3D cyan/teal/emerald) en la barra de la toolbar del chat de Jarvis, junto al toggle Turbo ⚡ y el picker de imagen 🎨. Abre modal listando los 12 TURBO_MODELS agrupados por proveedor (con providerIcon + tier badge vía getTierBadge) + opción "Auto" arriba. Estado `chatModel` (default 'auto') persiste en localStorage key `octopus_chat_llm_model`. Frontend: jarvis/page.tsx envía `modelOverride: chatModel` en el POST a /api/jarvis/chat (ambos: principal ~2591 y retry ~2744). Backend: route.ts destructura `modelOverride`, resuelve `overrideModel`/`overrideApiKey` (usa getUserOpenRouterKey() de turbo-llm.ts que obtiene la key del user SIN importar el flag turboEnabled → funciona aunque Turbo esté APAGADO). En callLLMRaw "PRIORIDAD 0": si hay overrideModel+key → fetch OpenRouter directo; si overrideModel sin key → body.model = toAbacusModel(override) para fallback Abacus. El override gana sobre Turbo (condición turbo lleva guard `!overrideModel`). turbo-llm.ts: exportados toAbacusModel + nuevo getUserOpenRouterKey. **Claude Fable 5 (June 12, 2026)**: agregado como modelo tope de Anthropic en TURBO_MODELS (id `anthropic/claude-fable-5`, tier ultra). "Fable 5" es el codename de Rafael (Claude Code Max), NO un slug público de OpenRouter → mapeado en toAbacusModel a `claude-opus-4-6` como fallback. Override mejorado: si OpenRouter rechaza el slug, ahora hace body.model = toAbacusModel(override) para servir el Claude real en Abacus en vez de gpt-4.1. Si Rafael consigue el slug real de OpenRouter para Fable 5, actualizar el id.
- **Code Engine optimization**: Secundario según Rafael, pero necesita mejora.
- **Canvas prompt v3**: DESPLEGADO (June 12, 2026) — Upgrade nivel agencia portando las recetas §11-§13 del Code Engine al buildCanvasPrompt() en lib/octopus-canvas.ts. Incluye: COLOR token system (:root, dark nunca #000, paletas por industria), TYPOGRAPHY (max 3 fuentes con roles + clamp), GLASSMORPHISM (.glass/.glass-liquid + header scroll-reactive), SHADOWS en capas, 5 variantes de BOTÓN con 4 estados, MOTION CATALOG (scroll reveal/marquee/particles/loading), COMPONENT BLUEPRINTS (navbar/hero/pricing/testimonials/FAQ/footer), IMAGES (SVG multi-capa 15-40 paths obligatorio porque Canvas NO genera imágenes reales — esta es la causa raíz del pulpo blob), FORMS demo (sin backend). Sección de diseño escrita en inglés (Rafael aprobó, mejor para las AIs). Contrato <<<CANVAS>>> + bloques file: intacto.
- **DIFERENCIA CLAVE Canvas vs Code Engine**: Code Engine tiene tool generate_image (imágenes IA reales) + Web3Forms. Canvas es solo bloques de texto, model-agnostic, SIN tool calls, SIN generación de imágenes → toda la calidad visual depende de SVG/CSS art a mano.

## Visual Style
- Primary: orange #C4622D
- Secondary: green #2D4A3E
- Background: cream #F5F0E8
- Dark mode supported

## Known Test False Positives (do NOT block checkpoint)
- Link x.com/octopuskills (external)
- Buttons in /sites/* (user-generated content)
- Contrast in /legal
- Duplicate logo images (intentional branding)
- Kangaroo image in user-created site

## OCTOPUS Personality
- Humor mexicano, honesto, directo, sin humo
- "Yo soy tu socio de negocios, tu CRM viviente"
- Anti-humo: grounding en datos reales, no inventa
- Introspección: se analiza a sí mismo con datos reales del sistema

## Claude Code Integration
- Rafael uses Claude Code (Fable 5/Max) for major renovations
- Code comes as ZIP from GitHub repo AIJAEL11/manifiesto (private)
- Process: ZIP → extract → compare schema → backup .env → replace code → restore config → build → test → deploy
